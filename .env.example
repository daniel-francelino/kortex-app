# Public URL, used for OG Image when running nuxt generate
NUXT_PUBLIC_SITE_URL=

# OneSignal (client-side, production only)
# Set to true only in production environments where push/web notifications should be enabled
NUXT_PUBLIC_ONESIGNAL_ENABLED=false
# Example: 11111111-2222-3333-4444-555555555555
NUXT_PUBLIC_ONESIGNAL_APP_ID=
# Keep defaults unless you intentionally move the OneSignal service worker files
NUXT_PUBLIC_ONESIGNAL_SERVICE_WORKER_PATH=/push/onesignal/OneSignalSDKWorker.js
NUXT_PUBLIC_ONESIGNAL_SERVICE_WORKER_UPDATER_PATH=/push/onesignal/OneSignalSDKUpdaterWorker.js
NUXT_PUBLIC_ONESIGNAL_SERVICE_WORKER_SCOPE=/push/onesignal/

# PostHog (client-side, production only)
# Set to true only in production environments where analytics should be enabled
NUXT_PUBLIC_POSTHOG_ENABLED=false
# Example: phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NUXT_PUBLIC_POSTHOG_KEY=
# Example: https://us.i.posthog.com
NUXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Supabase (server-side only)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY= your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Stripe (server-side only)
STRIPE_SECRET_KEY=
# Comma-separated allowlist (ex: price_123,price_456)
STRIPE_ALLOWED_PRICE_IDS=
# Optional: if you created a custom portal configuration
STRIPE_BILLING_PORTAL_CONFIGURATION_ID=

# Shared secret required by cron-shaped endpoints (server/utils/require-cron-secret.ts),
# checked against the `x-cron-secret` header. The external CRON API that schedules these
# jobs must send it. Leave unset only in local development — if unset, those endpoints
# have no authentication at all.
CRON_SECRET=

# Journal E2E encryption — public half of the support recovery keypair
# (docs/journal/PLANO_CRIPTOGRAFIA_PONTA_A_PONTA.md). Generate BOTH keys with
# `node scripts/generate-journal-recovery-keypair.mjs`. Paste only the
# PUBLIC key value into your real, untracked .env — NEVER a real value in
# this .example file (same convention as every other var above, which are
# all blank/placeholder templates). The PRIVATE key must never go in this
# file, this repo, or any .env the running app reads at all — it belongs in
# a separate secrets vault, outside this project entirely. Without the
# public key set, activating journal encryption fails for everyone.
NUXT_PUBLIC_JOURNAL_RECOVERY_PUBLIC_KEY=
